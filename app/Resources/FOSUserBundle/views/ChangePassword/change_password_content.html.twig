{% trans_default_domain 'FOSUserBundle' %}

<div class="container">
    <div class="row">

        <div class="col-sm-3">
            {% include 'BonPlanBundle:Default/layout:visiteurConnecte.html.twig' %}
        </div>
        <div class="col-sm-9">
            <h3  class="text-primary"> Changer mon mot de passe</h3>


            <div class="col-sm-8">
                <h4 class="text-secondary">Changer mon mot de passe</h4>
                <div class="form-group row">
                <form action="{{ path('fos_user_change_password') }} " method="POST" class="text-white">
                    {{ form_widget(form) }}

                    <input type="submit" value="{{ 'change_password.submit'|trans }}" class="btn btn-primary text-white"/>

                    {{ form_end(form) }}
                </form>
                </div>
            </div>
        </div>




    </div>
</div>
